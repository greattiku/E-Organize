import 'package:flutter/material.dart';

class ScheduleModel {
   final Widget? icon1;
  final String? title;
  final String? subTitle;
  final Widget? icon;
  final String? time;
  final String? progressLabel;
  final Widget? icon2;

  ScheduleModel({
     this.icon1,
     this.title,
     this.subTitle,
     this.icon,
     this.time,
     this.progressLabel,
     this.icon2,
  });
}